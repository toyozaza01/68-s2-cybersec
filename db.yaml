services:
  s2db:  
      image: postgres:latest
      container_name: 68-s2-db
      ports:
        - ${DATABASE_PORT}:5432
      environment:
        POSTGRES_USER: ${POSTGRES_USER}
        POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        POSTGRES_DB: ${POSTGRES_DB}